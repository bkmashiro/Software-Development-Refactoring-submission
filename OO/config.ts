/**
 * Represents the discount rates for different levels.
 */
export const discount = {
  /**
   * The discount rate for the normal level.
   */
  normal: 1,
  /**
   * The discount rate for the gold level.
   */
  gold: 0.9,
  /**
   * The discount rate for the platinum level.
   */
  platinum: 0.8,
}

/**
 * Represents the levels and their corresponding values (expenditure).
 */
export const level = {
  /**
   * The normal level with a value of 0.
   */
  normal: 0,
  /**
   * The gold level with a value of 1000.
   */
  gold: 1000,
  /**
   * The platinum level with a value of 10000.
   */
  platinum: 10000,
}
